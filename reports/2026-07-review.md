# Tariff data review — 2026-07-04 (baseline / first review)

Run interactively from Claude Code on Tim's machine; future reviews run as the
monthly cloud routine. This file is the baseline the next review diffs against.

## Data changes published

- **svt-price-cap** → Jul–Sep 2026 cap: 26.11p/kWh + 57.19p/day (Ofgem). The
  expired Apr–Jun entry (24.67p) had lingered because the Ofgem page still
  lists the outgoing period; the scraper now prunes ended periods. No Oct–Dec
  cap yet — Ofgem announces by 26 Aug 2026.
- **Tombstoned** (all superseded, verified 2026-07-04): `bg-fixed-jun27`
  (ismybillfair flags "not currently available"), `bg-longer-fix-may28`
  (no July successor found), `bg-cap-tracker-jun27` (re-based; replaced by
  BG "Tracker"), `svt-price-cap-next` (no announced next period).
- **Added**: `bg-tracker` (cap −£50/yr dual-fuel via standing-charge discount,
  derived SC), `bg-fixed-sep27` (regional midpoints — BG publishes no national
  average), `so-green-tracker` (£50/yr below cap, derived), `sp-cap-tracker`
  (small SC discount, sources conflict £7.50–£15/yr, no exit fees).
- **Skipped**: OVO (publishes no rates — quote-only). Market-leading fixes per
  MSE 30 Jun: Fuse (−17.5% vs cap), Outfox the Market (−16.5%), E.ON Next
  Fixed 12m Exclusive v3 (−15.2%) — E.ON priced live; Fuse/Outfox not added
  (no national-average p/kWh published; revisit).
- **closedToNew**: `["FLUX"]` unchanged — Flux/Intelligent Flux still paused
  (Octopus X post, June 2026); Octopus says it will reopen, re-check monthly.
  NOTE: octopus.energy/smart/flux/ still shows sign-up buttons — the page is
  stale, don't trust it over the X post/news.

## Kraken catalogue baseline (2026-07-04)

Counts: Octopus 37 products (28 own-brand; 11 import + 6 export candidates),
EDF 21 (14 import, **0 export — EDF still exposes none**), E.ON 104 (87
own-brand; 11 import + 3 export candidates after filters).

New families since June: Octopus **OUTGOING-PRIME-FIX-12M** (two-rate export,
16p 4–7pm / 9p), **COSY-FIX-12M**, **GO-FIX-12M**, **INTELLI-FIX(-OEV)-12M**,
**OE-FIX-12M**, **SNUG** (storage-heater ToU); EDF **EXCLUSIVE_FIX_JUL2027_V3**
(sold only via Uswitch/Confused), **SIMPLY_TRACKER_SEP2027**,
FREEPHASE_STATIC → V3; E.ON **NEXT_GUST_12M**, **NEXT_LOWER_DAILY_FIXED_24M**,
NEXT_FIXED_12M v137→v142, **NEXT_OPTIMISE_IMPORT_GAS** (solar+battery HH
tariff whose electricity leg is NOT in the public API — unpriceable, currently
dropped by the apps' GAS filter; revisit if an electricity code appears).

Gone from the API since June: **Octopus Tracker (SILVER-\*)**, **OUTGOING-LITE**,
variable GO / Intelligent Go (import range outside Agile/Cosy/Snug/Flexible is
now fixed-term only). All three suppliers' SVTs rolled to the July cap on
2026-06-30T23:00Z. EDF fixes now carry tranche `available_to` dates (normal).

## App-code changes — ALREADY APPLIED this review (in the apps, v1.17/1.9/1.8)

These shipped in the same session; listed so next review knows the classifier:
- battery regex no longer matches bare " storage" (Snug false positive);
  now `battery|home storage|energy storage`.
- New `third-party sign-up` tag for channel exclusives ("only to be sold
  through…"), distinct from hardware `exclusive`; Optimiser/Planner keep such
  tariffs eligible, Compare badges 🛒.
- Octopus VAR rows now duplicate per payment_method — directDebitOnly()
  already handled it (comment updated).
- Scraper prunes expired Ofgem periods + retires adapter-owned stale ids.
- Remote data channel added (this repo) — apps fetch other-tariffs.json from
  Pages at boot, newest `updated` wins, closedToNew ships as data.

## Open watch items for next review

1. FLUX reopening (remove from closedToNew when it happens).
2. Oct–Dec 2026 cap (announced by 26 Aug; scraper picks it up as
   svt-price-cap-next automatically — verify).
3. OUTGOING-PRIME two-rate export: confirm the apps price a fixed ToU export
   timeline correctly once someone selects it (engine path is the same as
   import ToU, believed fine, unverified).
4. NEXT_OPTIMISE electricity leg appearing in the public API.
5. BG Fixed Sep27 / Fix & Fall national-average rates (currently midpoint or
   missing — improve if a better source appears).
6. Fuse / Outfox the Market (market-leading fixes, no published national
   averages yet).
