# Tariff data review — 2026-08-01 — BLOCKED (2nd consecutive month)

## Data changes published

None. `other-tariffs.json` is byte-for-byte unchanged from the 2026-07-04
baseline (verified via `git diff` — no commit from this run touches it). See
root cause below.

## What happened

Ran the monthly review as scheduled. Step 1 (`node
scraper/fetch-other-tariffs.mjs`) failed exactly as it did last month:

```
✗ ofgemPriceCap: 403 for https://www.ofgem.gov.uk/information-consumers/energy-advice-households/energy-price-cap-unit-rates-and-standing-charges (keeping previous figures)
```

This is **not** an Ofgem page-layout problem. Confirmed by hand:

- `curl` CONNECT tunnels to `www.ofgem.gov.uk`, `api.octopus.energy`,
  `api.edfgb-kraken.energy`, `api.eonnext-kraken.energy`,
  `www.energyplus.co.uk`, `ismybillfair.com`, `www.moneysavingexpert.com` all
  return `403 Forbidden` at the proxy CONNECT stage (`gateway answered 403`),
  before any request reaches the target host.
- The `WebFetch` tool gets the same `403 Forbidden` fetching the Ofgem page
  directly.
- Three neutral control hosts (`example.com`, `www.google.com`,
  `en.wikipedia.org`) are **also** 403'd — this is a blanket egress policy
  for this environment, not a per-site or per-supplier block.
- Only `WebSearch` (Anthropic's own search backend, separate from this
  environment's general egress) works.

**This is the identical failure mode reported in the 2026-07-04 review**,
which already asked for `ofgem.gov.uk`, `api.octopus.energy`,
`api.edfgb-kraken.energy`, `api.eonnext-kraken.energy`, `energyplus.co.uk`,
`ismybillfair.com`, and `moneysavingexpert.com` to be allow-listed for this
scheduled routine's environment. That fix has evidently not been applied —
one month later, the block is unchanged. **Every future monthly firing will
keep hitting this wall until the environment's network policy is fixed; this
is not something fixable from inside the repo or the scraper code.**

Because steps 1–3 (Ofgem scraper, full Kraken catalogue pagination via
`api.octopus.energy`/`api.edfgb-kraken.energy`/`api.eonnext-kraken.energy`,
and non-Kraken supplier verification via energyplus/ismybillfair/MSE) all
require direct fetch, none of them could run this month either.

## What I did instead (WebSearch only, conservative)

Per the brief, never publish a figure without a checkable source — and a
WebSearch summary of a page is not the same as being able to fetch and read
that page myself, so I used it only to sanity-check figures **already** in
the data, not to source anything new:

- **Cap unchanged**: WebSearch results (energyplus.co.uk, Ofgem's own news
  post) consistently show the Jul–Sep 2026 cap as 26.11p/kWh electricity +
  57.19p/day standing charge — matches the existing `svt-price-cap` entry
  exactly, so no change needed regardless of the fetch failure.
  [Ofgem: changes to the price cap 1 Jul–30 Sep 2026](https://www.ofgem.gov.uk/news/changes-energy-price-cap-between-1-july-and-30-september-2026),
  [EnergyPlus: Energy Price Cap explained (Aug 2026)](https://www.energyplus.co.uk/guidesfaqs/energypricecap)
- **No Oct–Dec 2026 cap yet**: still expected by 26 Aug 2026 per multiple
  sources; nothing published as of today (1 Aug). Consistent with baseline —
  `svt-price-cap-next` correctly stays absent.
  [EnergyPlus: Ofgem price cap Q4 2026: when announced](https://www.energyplus.co.uk/local/ofgem-price-cap-q4-2026-when-announced-uk)
- **FLUX still closed to new sign-ups**: search results describe Flux and
  Intelligent Flux as closed since March 2026 with no confirmed reopening
  date as of the latest coverage found (May–June 2026); no evidence of the
  2026 reopening Octopus had signalled. `closedToNew: ["FLUX"]` left
  unchanged. [switchtooctopus.co.uk: Octopus Energy Tariff Changes March 2026](https://switchtooctopus.co.uk/articles/octopus-tariff-changes-march-2026/)

I deliberately did **not** touch the Kraken catalogue audit, closedToNew
beyond the above, or any non-Kraken tariff entry — none of those can be
verified against a source I can actually fetch and read from this
environment, and unverifiable figures must never be published.

### Unconfirmed lead for next review (not acted on)

A WebSearch snippet surfaced an EnergyPlus page titled **"Cap Tracker Dec
2026 A1 | ScottishPower"**, alongside pages still describing the plain "Cap
Tracker" launched June 2026 (our `sp-cap-tracker` entry, sourced 2026-07-04).
This naming pattern (`<name> <month><year> <version>`) matches how British
Gas's tracker got re-based and renamed last cycle (`bg-cap-tracker-jun27` →
tombstoned, replaced by `bg-tracker`). It's plausible ScottishPower's tracker
has similarly been re-issued under a new id/rate since July. **Not enough to
act on** — I cannot fetch the EnergyPlus or ScottishPower page to confirm
the actual rate, only see a search-result title. Next review: check
`https://www.scottishpower.co.uk/tracker` and the EnergyPlus tariff page
directly once fetch access exists; tombstone `sp-cap-tracker` and add a
successor if it's confirmed re-based.

## Kraken catalogue audit

Not performed — blocked (see above). No product-catalogue changes to report
this month; baseline counts from 2026-07-04 (Octopus 37, EDF 21, E.ON 104)
are carried forward unverified. FLUX status re-checked via WebSearch only
(see above).

## NEEDS APP-CODE / INFRA CHANGES

- **Network egress allow-list — STILL UNRESOLVED, now 2 months running.**
  The scheduled cloud routine's environment needs `ofgem.gov.uk`,
  `api.octopus.energy`, `api.edfgb-kraken.energy`,
  `api.eonnext-kraken.energy`, `energyplus.co.uk`, `ismybillfair.com`,
  `moneysavingexpert.com`, and relevant individual supplier domains
  (`britishgas.co.uk`, `so.energy`, `scottishpower.co.uk`) added to its
  egress policy. Until this is fixed, the monthly review can only ever do a
  WebSearch-based sanity check of existing figures — it can never source a
  genuinely new rate, catch a Kraken catalogue change, or run the scraper.
  This is an environment/infrastructure fix, not something the repo or
  scraper code can work around. Recommend checking whether this routine's
  environment is using a different network policy than the interactive
  session that produced the 2026-07-04 baseline (that session, run
  interactively on Tim's machine, evidently had working egress — this
  scheduled one has not, in either of its two firings so far).

## Open watch items carried forward

1. FLUX reopening — still watching, no date yet.
2. Oct–Dec 2026 cap — expected by 26 Aug 2026, scraper will pick it up
   automatically as `svt-price-cap-next` once egress is fixed.
3. ScottishPower Cap Tracker possible re-basing — see "Unconfirmed lead"
   above.
4. OUTGOING-PRIME two-rate export engine-path verification (unverified since
   baseline).
5. NEXT_OPTIMISE electricity leg appearing in the public API (unverified
   since baseline — Kraken audit blocked).
6. BG Fixed Sep27 / So Green Tracker / Fuse / Outfox national-average
   sourcing improvements (unverified since baseline).
